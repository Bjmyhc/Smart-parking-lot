
# 智能停车场系统架构优化方案

## 一、当前架构分析

### 1.1 当前系统架构图

```
当前架构（直接WiFi模式）:
┌─────────────────────────────────────────────────────────┐
│                    STM32F103 主控                        │
├─────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 超声波驱动   │  │ 地磁QMC5883P │  │ OLED显示     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 车位状态机   │  │ LED控制      │  │ WiFi任务     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
│         USART2 (PA2/PA3, 115200bps)                     │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    ESP8266 WiFi模块                      │
├─────────────────────────────────────────────────────────┤
│  • STA模式连接路由器                                    │
│  • TCP透传模式连接OneNET (1883端口)                     │
│  • AT指令控制 (初始化/重连时)                            │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
                    云平台 (OneNET)
```

### 1.2 当前架构局限性

| 问题                      | 说明                                |
| ------------------------- | ----------------------------------- |
| **ESP8266无处理器** | ESP8266只是WiFi模块，不处理业务逻辑 |
| **STM32负载过重**   | 同时处理传感器、显示、通信协议      |
| **无多任务调度**    | 裸机循环，所有任务串行执行          |
| **扩展能力差**      | 增加摄像头/AI功能需完全重构         |
| **故障隔离弱**      | 通信故障影响传感器采集              |
| **无可靠心跳**      | MQTT连接长时间空闲易被服务器断开    |
| **重连机制不完善**  | 断连后需手动重启ESP8266才能恢复     |

---

## 二、推荐新架构：ESP32 主网关架构

### 2.1 新架构总体设计

```
┌──────────────────────────────────────────────────────────────────────┐
│                        新架构方案                                      │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                    ESP32 主网关（核心）                      │    │
│  │  ┌─────────────────────────────────────────────────────┐   │    │
│  │  │                 FreeRTOS 任务调度                   │   │    │
│  │  ├─────────────────────────────────────────────────────┤   │    │
│  │  │ • WiFi管理任务    • MQTT通信任务   • HTTP服务器任务 │   │    │
│  │  │ • 数据转发任务    • OTA升级任务    • 故障诊断任务   │   │    │
│  │  ├─────────────────────────────────────────────────────┤   │    │
│  │  │                 中间件服务层                         │   │    │
│  │  │ • 设备管理服务    • 数据存储服务   • 消息队列服务   │   │    │
│  │  │ • 协议适配服务    • 安全认证服务   • 日志服务       │   │    │
│  │  ├─────────────────────────────────────────────────────┤   │    │
│  │  │                 外设驱动层                           │   │    │
│  │  │ • WiFi驱动  • BLE驱动  • Ethernet驱动  • GPIO/I2C  │   │    │
│  │  └─────────────────────────────────────────────────────┘   │    │
│  │                                                             │   │    │
│  │   WiFi / BLE / Ethernet                                    │   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐              │
│  │  STM32      │    │ LoRa模块    │    │ 摄像头模块  │              │
│  │ 传感器节点  │    │ (可选)      │    │ (ESP32-CAM) │              │
│  ├─────────────┤    ├─────────────┤    ├─────────────┤              │
│  │ • 超声波    │    │ 远距离无线  │    │ • 图像采集  │              │
│  │ • 地磁      │    │ 节点通信   │    │ • AI识别    │              │
│  │ • 温度湿度  │    │ 中继功能   │    │ • 车牌识别  │              │
│  └─────────────┘    └─────────────┘    └─────────────┘              │
│        ▲                   ▲                   ▲                     │
│        │ UART/I2C          │ UART              │ WiFi/SDIO           │
│        └───────────────────┼───────────────────┘                     │
│                            │                                        │
│                    ESP32 外设接口                                     │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块功能划分

#### 2.2.1 ESP32 主网关（核心控制器）

| 模块名称    | 功能描述                                     |
| ----------- | -------------------------------------------- |
| WiFiManager | WiFi连接管理、自动重连、AP模式切换           |
| MQTTClient  | MQTT连接、心跳保活、消息收发、断线重连       |
| HttpServer  | 本地Web管理界面、设备配置、状态监控          |
| DataRouter  | 数据路由：传感器数据→云平台、云端指令→设备 |
| DeviceMgr   | 设备管理：传感器节点发现、注册、监控         |
| StorageMgr  | 数据存储：Flash/SD卡缓存、离线数据暂存       |
| OTAManager  | 固件升级：远程OTA、版本管理、回滚支持        |
| Diagnostics | 故障诊断：网络状态、设备状态、自动恢复       |
| SecurityMgr | 安全管理：设备认证、数据加密、访问控制       |

#### 2.2.2 STM32 传感器节点（协处理器）

| 模块名称     | 功能描述                             |
| ------------ | ------------------------------------ |
| SensorCtrl   | 传感器控制：超声波、地磁、温湿度采集 |
| DataProc     | 数据预处理：滤波、校准、异常检测     |
| StateMachine | 车位状态机：空闲/有车/僵尸车状态管理 |
| CommIf       | 通信接口：与ESP32的UART通信协议      |
| LocalCtrl    | 本地控制：OLED显示、LED控制、蜂鸣器  |
| LowPower     | 低功耗管理：待机模式、定时唤醒       |

---

## 三、数据流向说明

### 3.1 上行数据流（传感器→云端）

```
┌──────────┐   I2C/UART   ┌──────────┐   UART/SPI   ┌──────────┐   WiFi    ┌──────────┐
│ 超声波   │─────────────►│ STM32    │────────────►│ ESP32    │─────────►│ 云平台   │
│ 地磁     │              │ 节点     │              │ 网关     │          │          │
└──────────┘              └──────────┘              └──────────┘          └──────────┘
                                │                          │
                                │ OLED/LED                 │ HTTP API
                                ▼                          ▼
                         [本地显示]                 [Web管理界面]
```

**数据流详细说明：**

1. **传感器采集层**

   - 超声波(HC-SR04)、地磁(QMC5883P)等传感器连接到STM32
   - STM32以100ms周期采集数据
2. **数据处理层 (STM32)**

   - 数据预处理：滤波(EMA)、校准、异常值剔除
   - 状态判定：车位状态机（空闲/有车/僵尸车）
   - 封装数据：按约定格式打包
3. **传输层 (STM32↔ESP32)**

   - 使用Modbus或自定义协议通过UART通信
   - 支持命令/响应模式
   - 支持事件触发上报
4. **网关层 (ESP32)**

   - 数据聚合：收集多个节点数据
   - 协议转换：MQTT/DTLS加密
   - 本地存储：离线数据缓存
5. **云端层**

   - 数据存储：时序数据库
   - 数据分析：车位统计、占用率分析
   - 消息推送：状态变化通知

### 3.2 下行数据流（云端→设备）

```
┌──────────┐   MQTT     ┌──────────┐   UART   ┌──────────┐
│ 云平台   │────────────►│ ESP32    │────────►│ STM32    │
│          │ 下行指令    │ 网关     │ 转发    │ 节点     │
└──────────┘              └──────────┘         └──────────┘
                               │                    │
                               │ Web管理            │ 执行控制
                               ▼                    ▼
                        [本地配置]           [控制外设]
```

### 3.3 设备间通信协议

```
STM32 ↔ ESP32 通信协议设计:
┌──────┬──────┬──────┬──────┬──────────────┐
│ 帧头 │ 命令 │ 长度 │ 校验 │  数据负载    │
│ 2字节│ 1字节│ 2字节│ 1字节│  N字节       │
└──────┴──────┴──────┴──────┴──────────────┘

命令定义:
  0x01: 数据上报 (STM32→ESP32)
  0x02: 数据请求 (ESP32→STM32)
  0x03: 控制命令 (ESP32→STM32)
  0x04: 状态响应 (STM32→ESP32)
  0x05: 心跳探测 (双向)
  0x10: 配置写入 (ESP32→STM32)
  0x11: 配置读取 (ESP32→STM32)
```

---

## 四、关键技术实现方案

### 4.1 ESP32 自动重连机制

```c
// WiFiManager 核心实现思路
class WiFiManager {
private:
    enum State {
        IDLE,           // 空闲
        CONNECTING,     // 连接中
        CONNECTED,      // 已连接
        RECONNECTING,   // 重连中
        AP_MODE         // AP模式(故障时)
    };
  
    State state;
    uint32_t lastReconnectAttempt;
    uint8_t retryCount;
    uint32_t backoffDelay;  // 指数退避
  
public:
    // 自动重连策略
    void handleReconnect() {
        switch(state) {
            case CONNECTED:
                // 检查WiFi连接质量
                if (WiFi.RSSI() < -75 || !WiFi.isConnected()) {
                    transitionTo(RECONNECTING);
                }
                break;
            
            case RECONNECTING:
                // 指数退避: 1s, 2s, 4s, 8s, 16s, 30s(max)
                backoffDelay = min(30000, 1000 * (1 << retryCount));
            
                if (millis() - lastReconnectAttempt > backoffDelay) {
                    WiFi.reconnect();
                    lastReconnectAttempt = millis();
                    retryCount++;
                
                    if (retryCount > MAX_RETRIES) {
                        // 尝试AP模式(手机可连接配置)
                        transitionTo(AP_MODE);
                    }
                }
                break;
            
            case AP_MODE:
                // 等待用户配置或网络恢复
                if (WiFi.status() == WL_CONNECTED) {
                    transitionTo(CONNECTED);
                }
                break;
        }
    }
  
    // MQTT心跳保活
    void keepAlive() {
        if (millis() - lastPingTime > KEEPALIVE_INTERVAL) {
            mqttClient.ping();
            lastPingTime = millis();
        
            if (!mqttClient.waitForPong(3000)) {
                mqttClient.disconnect();
                transitionTo(RECONNECTING);
            }
        }
    }
};
```

### 4.2 系统故障自恢复策略

```
┌─────────────────────────────────────────────────────────────────┐
│                     故障自恢复状态机                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [正常运行] ──► [网络异常] ──► [重连尝试] ──► [恢复成功]       │
│     ▲                                          │                │
│     │                                          │失败            │
│     │                                          ▼                │
│     │                               [尝试AP模式]                │
│     │                                          │                │
│     │                                          │超时            │
│     │                                          ▼                │
│     └──────────────────────────────[系统重启] ◄──┘               │
│                                                                 │
│  检测方式:
│  1. WiFi连接状态 (WiFi.status())
│  2. MQTT心跳响应 (ping/pong)
│  3. 数据收发超时 (lastTxTime / lastRxTime)
│  4. 系统运行时间 (uptime check)
│                                                                 │
│  恢复策略:
│  1. 轻量恢复: 重建MQTT连接 (1s)
│  2. 中度恢复: 重连WiFi+MQTT (5s)
│  3. 重度恢复: ESP32软件复位 (30s)
│  4. 灾难恢复: 看门狗硬件复位 (1min)
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 多任务架构设计

```cpp
// FreeRTOS 任务分配
void setup() {
    // 创建各功能任务
    xTaskCreate(WiFiTask, "WiFi", 4096, NULL, 1, NULL);      // 高优先级
    xTaskCreate(MQTTTask, "MQTT", 4096, NULL, 2, NULL);      // 高优先级
    xTaskCreate(DataTask, "Data", 8192, NULL, 3, NULL);      // 中优先级
    xTaskCreate(HttpTask, "HTTP", 8192, NULL, 1, NULL);      // 低优先级
    xTaskCreate(StorageTask, "Storage", 4096, NULL, 2, NULL); // 中优先级
    xTaskCreate(WatchdogTask, "WD", 1024, NULL, 0, NULL);    // 监控任务
  
    // 创建消息队列(任务间通信)
    sensorQueue = xQueueCreate(10, sizeof(SensorData));
    commandQueue = xQueueCreate(10, sizeof(Command));
}

// 消息队列通信示例
void DataTask(void* param) {
    SensorData data;
    while(true) {
        // 从传感器队列获取数据
        if (xQueueReceive(sensorQueue, &data, portMAX_DELAY)) {
            // 数据处理和上传
            processAndUpload(data);
        }
    }
}
```

### 4.4 扩展模块接口

| 模块类型   | 接口方式                               |
| ---------- | -------------------------------------- |
| 摄像头模块 | WiFi连接ESP32, 通过RTSP/HTTP推送视频流 |
| AI模块     | 本地推理(ESP32-CAM)或云端API调用       |
| LoRa模块   | UART接口, 支持远距离节点通信           |
| 4G模块     | UART/SPI接口, 支持蜂窝通信(备选)       |
| BLE网关    | 支持BLE传感器节点接入                  |
| USB扩展    | 支持USB设备(如U盘、打印机)             |

---

## 五、两种架构对比

| 对比维度             | 当前架构 (STM32+ESP8266) | 新架构 (ESP32主网关) |
| -------------------- | ------------------------ | -------------------- |
| **处理能力**   | STM32F103: 72MHz         | ESP32: 240MHz双核    |
| **内存**       | 20KB RAM                 | 520KB+ PSRAM         |
| **WiFi功能**   | ESP8266透传模式          | ESP32原生WiFi+BLE    |
| **多任务**     | 裸机循环                 | FreeRTOS多任务       |
| **扩展能力**   | 有限                     | 丰富的外设接口       |
| **AI能力**     | 无                       | 支持TensorFlow Lite  |
| **升级方式**   | 需要编程器               | 远程OTA升级          |
| **成本**       | 较低                     | 略高                 |
| **开发复杂度** | 较低                     | 中等                 |
| **可靠性**     | 一般                     | 高(多层恢复机制)     |

---

## 六、迁移路径建议

### 阶段1: 修复当前系统（推荐立即执行）

- 添加MQTT心跳机制
- 实现连接健康监测
- 优化重连逻辑
- 预期可将离线恢复时间从30s缩短至5s内

### 阶段2: 引入ESP32作为辅助处理器（1-2周）

- ESP32负责WiFi/MQTT通信
- STM32保留传感器采集功能
- UART通信协议对接
- 实现双通道冗余

### 阶段3: 全面迁移到ESP32架构（2-4周）

- ESP32接管所有功能
- 添加OTA升级功能
- 实现Web管理界面
- 完成测试和验证

### 阶段4: 功能扩展（按需）

- 接入摄像头模块
- 实现AI车位检测
- 增加LoRa远距离节点
- 构建完整的停车场管理系统

---

## 七、总结与建议

### 立即执行的修复（当前架构优化）

1. 添加MQTT心跳机制（每120秒PINGREQ）
2. 实现连接健康监测（最后通信时间戳+超时判定）
3. 优化重连逻辑（分级恢复：轻量/重度）
4. 添加WiFi/TCP状态检测功能

### 长期架构优化建议

1. **短期（1-2个月）**：修复当前架构问题，确保系统稳定运行
2. **中期（3-6个月）**：引入ESP32作为核心网关，实现多任务架构
3. **长期（6个月+）**：构建完整的IoT平台，支持AI、摄像头等扩展功能

### 风险评估

- **低风险**：在当前STM32+ESP8266架构上添加心跳和重连机制
- **中风险**：引入ESP32作为辅助处理器，需要协调双处理器通信
- **高风险**：全面迁移到新架构，需要大量测试验证
